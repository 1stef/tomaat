{% extends 'base.html.twig' %}

{% block title %}Geef nieuw wachtwoord op{% endblock %}

{% block content %}
{{ parent() }}
<div class="w3-container narrow">
    <h1>Geef nieuw wachtwoord op</h1>

    {% form_theme resetForm 'bootstrap_4_layout.html.twig' %}
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="newbutton">Verander wachtwoord</button>
    {{ form_end(resetForm) }}
</div>
{% endblock %}
